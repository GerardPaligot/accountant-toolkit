{
  "name": "accountant-toolkit",
  "version": "0.1.0",
  "private": true,
  "description": "Accountant workspace toolkit: a Claude Code plugin (skills) + CLIs (file validator, Revolut API client) + schemas.",
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "author": { "name": "Gérard Paligot" },
  "license": "UNLICENSED"
}
